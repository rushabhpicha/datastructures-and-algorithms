class Solution {
    public int findNumbers(int[] nums) {
        int ans = 0;
        if(nums.length == 0) return 0;
        for(int i = 0; i<nums.length; i++){
            String demo = String.valueOf(nums[i]);
            if(demo.length() % 2 == 0) ans++;
        }
        return ans;
    }
}